# ==========================================================
# localhostの接続
# ==========================================================
spring:
  datasource:
    url: jdbc:mysql://localhost:3300/sampleDB
    username: root
    password: root
# ==========================================================